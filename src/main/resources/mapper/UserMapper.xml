<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myshop.mapper.UserMapper"> <select id="findByUsername" resultType="User" parameterType="string">
        SELECT
            id,
            username,
            password_hash,
            email,
            created_at,
            updated_at
        FROM
            t_user
        WHERE
            username = #{username}
    </select>

</mapper>